# 社媒自动化发布项目总结（Meta + TikTok 双端）

> 更新时间：2026-08-20
> 项目：飞书 → AdsPower 指纹浏览器 → Meta(FB+IG) / TikTok 自动发布

## 一、项目目标

飞书多维表格「捏捏乐独立站」中的视频，自动生成英文文案后，一键发布到 Meta（Facebook + Instagram）和 TikTok，并将发布状态回写飞书。

## 二、最终成果（2026-08-20 全部 4 条发布完成）

| # | 视频 | 标题 | Meta | TikTok | 飞书 |
|---|------|------|------|--------|------|
| 1 | 0802-02(1).mp4 (69MB) | Zero calories, 100% stress relief 🍰✨ | ✅ | ✅ | ✅ 已发布 |
| 2 | 0818-01(2).mp4 (50MB) | Do NOT eat this cake! 🍰🤫 | ✅ | ✅ | ✅ 已发布 |
| 3 | 8月18日(1)最新.mp4 (66MB) | Melt your day away 🍰✨ | ✅ | ✅ | ✅ 已发布 |
| 4 | 8月18日 (1).mp4 (58MB) | Turn your volume all the way up 🎧✨ | ✅ | ✅ | ✅ 已发布 |

## 三、核心流程（验证过的最终版）

```
飞书读任务 → 下载视频到 Downloads/feishu_videos/<表名>/<record_id>.mp4
→ Meta: composer 上传(真实点击+setFileInputFiles) → 真实点击文案框+insertText → 下一页 → 立即分享 → 底部分享
→ TikTok: 上传(DOM直连input) → 标题(真实键盘) → 标签(逐个+弹窗后1.5s回车) → Post
→ 飞书状态回写「已发布」
```

### 关键技术点

1. **CDP websocket 直连**：读 `D:\.ADSPOWER_GLOBAL\cache\k1dqriqs_h1msnw4\DevToolsActivePort`（浏览器重启后端口会变，必须现读）
2. **上传 >50MB 视频**：`DOM.setFileInputFiles` 本地路径直传（无 50MB 限制），Meta 用 chooser 事件，TikTok 用 `DOM.querySelector('input[type=file]')` 直连
3. **真实鼠标点击**（`Input.dispatchMouseEvent`）：React 应用（Meta/TikTok）不认 JS `.click()`，必须模拟真人鼠标事件
4. **真实键盘输入**（`Input.dispatchKeyEvent` 逐字符）：TikTok Draft.js 对 `insertText` 免疫（LEN=0），只认真实键盘事件
5. **NO_PROXY 陷阱**：websockets 连 localhost CDP 会被系统代理(127.0.0.1:10808)拦截 → 必须 `NO_PROXY="127.0.0.1,localhost"` 环境变量

## 四、⚠️ 弯路与错误记录（防重犯）

> 用户原话："把走的弯路和错误的问题都写上，不然下次又犯错"

### 1. TikTok 文案顺序错乱（标签插到正文中间）
- **现象**：正文输入后继续输标签，标签出现在 "Comment your favorite below!" 后，正文后半段被挤到末尾
- **根因**：Draft.js 光标坑 —— 正文输入后光标不在末尾，后续输入插到中间
- **解法**：**TikTok 只填「标题+标签」，不要正文**（用户 2026-08-20 新规）

### 2. TikTok 标签回车太快不生效（复现 3 次！）
- **现象**：标签输入后立即回车，标签不变蓝（`links=[]`），被当成普通换行
- **根因**：热度弹窗出现后标签还没完全渲染，回车太早
- **解法**：**弹窗出现后等 1.5 秒再回车**（先试 2s 用户说太慢 → 改 1.5s 定稿）；标签前**不要输空格**

### 3. execCommand('delete') 搞乱 Draft.js 状态
- **现象**：用 execCommand 补字符/删字符后，已删除的旧文本重新渲染，两段内容叠加（len 从 118 变 287）
- **解法**：**禁用 execCommand 做删除**；犯错后不要修补（补字符越修越乱）→ 直接 Ctrl+A 全选删除清空重来

### 4. 标题丢开头字符
- **现象**："Melt your day away" 输入后丢了 "Melt y"
- **根因**：清空后编辑器未完全就绪就开始输入，前几个键盘事件被吞
- **解法**：清空后等 1 秒，慢速输入（60ms/字符）

### 5. Meta 分享点错按钮
- **现象**：点「分享」点到了顶部导航 tab (269,78) 而不是底部发布按钮
- **解法**：先点「立即分享」radio，再点**底部**分享按钮（取 y>1000 的最后一个）

### 6. 浏览器重启后 CDP 连不上
- **现象**：端口变了（60571→52548→56951），websockets 报 "did not receive a valid HTTP response"
- **两个根因**：① 端口必须现读 DevToolsActivePort；② **代理拦截 localhost**（websockets 库走 connect_http_proxy）→ 加 `NO_PROXY="127.0.0.1,localhost"`

### 7. SunBrowser 窗口最小化（用户看不到操作）
- **现象**：用户看不到操作过程（窗口 minimized 在屏幕外 -32000）
- **解法**：cua-driver `bring_to_front` 恢复窗口；用户要求操作必须在 AdsPower 可见窗口内

### 8. 弹窗处理 Discard 陷阱
- **现象**：点 Discard 删除草稿后还要二次确认，循环弹窗卡死
- **解法**：**不要点 Discard，点 Continue 继续编辑**（草稿内容不用管，直接发布）

### 9. TikTok 上传 chooser 捕获失败（2 次）
- **现象**：`Page.fileChooserOpened` 事件捕获不到
- **解法**：改用 `DOM.getDocument` + `DOM.querySelector('input[type=file]')` 直连（**DOM.* 命令必须带 sessionId**，否则 KeyError: 'result'）

### 10. 弹窗检测 JSON 引号 bug
- **现象**：`json.dumps('#'+tag)` 带引号永远匹配不上弹窗文本
- **解法**：用**裸字符串** `tag_clean in popup` 判断

### 11. 验证脚本找错元素（误报失败）
- **现象**：TikTok 标签其实已变蓝，但脚本查 `document.querySelectorAll('a')` 返回 0
- **解法**：**用视觉确认**（ds-vision-skill / GLM）看截图，不依赖 DOM 检测

### 12. 大文件上传轮询输出爆炸
- **现象**：轮询时打印整个页面文本，输出太大卡住
- **解法**：只打印百分比，不打印全文

## 五、技能沉淀

- `adspower-cdp-automation`（主技能）：CDP 要点、上传、填文案、标签、弹窗、发布全流程
- `squishy-factory-copy`：文案生成、TTS、渲染链路（含 publish_dual.py 双发脚本）
- `ds-vision-skill`（新装）：DeepSeek 视觉能力，GLM 降级链（glm-4.6v→glm-thinking→glm-4v→comfly 兜底），用于截图确认
- 脚本目录：`~/Downloads/feishu_videos/`（tk_v5~v10、meta_publish_v2、publish_full 等）

## 六、配置备忘

- AdsPower profile: `k1dqriqs`，user-data: `D:\.ADSPOWER_GLOBAL\cache\k1dqriqs_h1msnw4`
- Meta: asset_id=`890016627535163`, business_id=`3521484561351336`
- 飞书 Base: `${FEISHU_BASE_TOKEN}`, 表: `tblJ1dgoGgNFoBIK`（捏捏乐独立站）
- 标签：TikTok 用 `fld0Qx2af5`（用户默认勿覆盖），Meta 用 `fldJ7j365Y`（生成写入）
